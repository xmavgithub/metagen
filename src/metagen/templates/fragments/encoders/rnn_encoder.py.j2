# RNN encoder fragment.
import torch
import torch.nn as nn


class RNNEncoder(nn.Module):
    def __init__(
        self,
        hidden_size: int = {{ blueprint.dims.hidden_size }},
        layers: int = max(1, {{ blueprint.dims.layers }} // 2),
    ) -> None:
        super().__init__()
        self.rnn = nn.GRU(
            input_size=hidden_size,
            hidden_size=hidden_size,
            num_layers=layers,
            batch_first=True,
        )

    def forward(self, x: torch.Tensor) -> torch.Tensor:
        output, _ = self.rnn(x)
        return output
